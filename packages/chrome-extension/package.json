{
  "name": "@mcp-pointer/chrome-extension",
  "version": "0.3.0",
  "private": true,
  "scripts": {
    "clean": "rm -rf dist",
    "clean-dev": "rm -rf dev",
    "typecheck": "tsc --noEmit src/*.ts",
    "build": "pnpm clean && esbuild src/background.ts src/bridge.ts src/content.ts src/popup.ts src/styles.css src/popup.css src/manifest.json src/popup.html --bundle --outdir=dist --outbase=src --format=iife --platform=browser --target=chrome100 --minify --define:IS_DEV=false --loader:.css=copy --loader:.html=copy --loader:.json=copy",
    "dev": "pnpm clean-dev && esbuild src/background.ts src/bridge.ts src/content.ts src/popup.ts src/styles.css src/popup.css src/manifest.json src/popup.html --bundle --outdir=dev --outbase=src --format=iife --platform=browser --target=chrome100 --sourcemap --define:IS_DEV=true --loader:.css=copy --loader:.html=copy --loader:.json=copy --watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.4",
    "esbuild": "catalog:",
    "typescript": "catalog:"
  },
  "dependencies": {
    "@mcp-pointer/shared": "workspace:*",
    "reconnecting-websocket": "^4.4.0"
  }
}
