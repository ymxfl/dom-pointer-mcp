---
"@dom-pointer-mcp/chrome-extension": patch
"@dom-pointer-mcp/server": patch
---

Wait for a server persistence ACK before reporting a selection as sent, show a success toast after confirmation, hide extension UI while capturing screenshots, attach saved screenshots as MCP image content, preserve visible/full text separately, filter kebab-case CSS correctly, move shared state to the platform temp directory, and report the real CLI/MCP server version with strict port validation.
