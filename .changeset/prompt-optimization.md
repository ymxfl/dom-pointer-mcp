---
"@dom-pointer-mcp/server": patch
---

fix: remove auto-trigger from skill description, unify skill/command prompt content, fix /pointed get skipping user confirmation, fix inconsistent cssLevel defaults, slim down MCP tool descriptions to reduce token usage
